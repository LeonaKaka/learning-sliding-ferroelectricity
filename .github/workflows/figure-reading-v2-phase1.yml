name: Figure Reading V2 phase 1

on:
  push:
    paths:
      - 'scripts/figure_reading_v2_phase1.py'
      - '.github/workflows/figure-reading-v2-phase1.yml'

permissions:
  contents: write

jobs:
  improve:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install parser
        run: python -m pip install --disable-pip-version-check beautifulsoup4
      - name: Apply Figure Reading V2 phase 1
        run: python scripts/figure_reading_v2_phase1.py
      - name: Verify only 04-06 changed
        run: |
          changed="$(git diff --name-only)"
          printf '%s\n' "$changed"
          bad="$(printf '%s\n' "$changed" | grep -vE '^(modules/(pinning-creep|depinning|disorder-rfim)\.html)?$' || true)"
          if [ -n "$bad" ]; then
            echo "Unexpected changed files:"
            echo "$bad"
            exit 1
          fi
          test -n "$changed"
      - name: Commit Figure Reading V2 pages
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add modules/pinning-creep.html modules/depinning.html modules/disorder-rfim.html
          git commit -m 'Add Figure Reading V2 guides to modules 04-06'
          git push
